# ── 파이썬 ────────────────────────────────────────────────────────────────────
.venv/
.venv-xtts/
__pycache__/
*.pyc
.pytest_cache/

# ── Node ──────────────────────────────────────────────────────────────────────
node_modules/
dist/
*.tsbuildinfo
.vite/

# ── PyInstaller 산출물 ────────────────────────────────────────────────────────
# EXE 배포본은 수 GB라 저장소에 넣지 않는다. Releases로 배포한다.
build/
server/dist/
*.manifest

# ── 실험 산출물 ───────────────────────────────────────────────────────────────
# 보호본·복제음·리포트. 재현 가능하므로 커밋하지 않는다.
out/
sweep_result.json
*.wav

# ── 모델 가중치 ───────────────────────────────────────────────────────────────
# Whisper·딥보이스 탐지기는 첫 실행에 받아서 캐시된다.
*.pt
*.bin
*.safetensors

# ── 로컬 설정 ─────────────────────────────────────────────────────────────────
.claude/
.vscode/
.idea/

# ── 이전 프로토타입 ───────────────────────────────────────────────────────────
# Figma Make 내보내기. app-desktop / app-mobile 이 대체했다.
*.zip
