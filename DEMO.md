# 시연 영상 촬영 가이드 — 모드 2 (딥보이스 학습 방지)

모드 1은 화면에서 위험도가 올라가고 경고가 뜨는 것이 그대로 보인다.
**모드 2는 다르다. 보이는 것이 없다.** 섭동은 들리지 않아야 하고, 효과는 숫자로만 나타난다.
그래서 촬영 설계가 훨씬 중요하다.

그리고 하나 먼저 못박아야 한다.

> **복제 실패 장면은 찍을 수 없다.**
> 보호본을 XTTS-v2에 넣어도 복제는 성공한다. DSR 0% (n=5, 원본·보호본·잡음 대조군 전부).
> 계획서(D07 §04)가 시연의 클라이맥스로 상정한 장면이 **현재 존재하지 않는다.**
> 이걸 모르고 촬영에 들어가면 현장에서 대본을 다시 써야 한다.

찍을 수 있는 것과 없는 것을 먼저 갈라 놓고 시작한다.

| | 상태 |
|---|---|
| 화자 검증기가 "다른 사람"으로 판정 | **된다** — 보호본 SRS 0.634, 임계값 0.796 |
| 같은 SNR 잡음보다 낫다 | **된다** — C-B 0.769 · C-A 0.805 대비 |
| 효과의 원인이 구조다 | **된다** — C-E(셔플) 0.890으로 거의 무효 |
| 통화 채널을 통과해도 유지 | **된다** — 채널 통과 후 0.5915 |
| 실시간 처리 | **된다** — CPU 15스텝 0.91초/청크 (경계선) |
| **XTTS 복제 실패** | **안 된다** — DSR 0% |
| **안 들린다** | **보장 못 한다** — 배율 3.0에서 bin의 20.3%가 마스킹 임계값 초과 |

---

## 촬영 전 반드시 끝내야 할 것

### 1. 팀원 실제 녹음 확보 — 이게 제일 중요하다

**XTTS 합성음으로 찍으면 안 된다.** 지금까지의 모든 수치가 XTTS 내장 화자 기반이고,
그건 "XTTS가 만든 것을 XTTS가 복제한다"라서 복제가 유난히 쉬운 조건이다(기준선 0.95).
심사위원이 "대상 음성이 뭐냐"고 물으면 답이 궁색해진다.

- 조용한 방, 6초 이상, 가능하면 30초 이상
- 시연 대본과 **다른 문장**으로 녹음할 것 (복제 대상과 복제 문장이 같으면 반칙으로 보인다)
- 16 kHz 모노 WAV. 휴대폰 녹음도 괜찮다

녹음이 들어오면 지금까지의 측정을 **전부 다시 돌린다.** 명령은 그대로다.

```bash
cd server
python protect.py 팀원녹음.wav -o out/demo --steps 200
python calibrate_dsr.py out/speakers --reference out/demo/original.wav \
       --compare out/demo/protected.wav out/demo/control_C-B.wav out/demo/control_C-E.wav
python codec_test.py out/demo
```

### 2. 마스킹 배율을 **귀로** 정한다

이게 촬영 전 가장 위험한 미결 사항이다.

| 배율 | 마스킹 임계값 위반 bin | 잡음 대비 |
|---|---|---|
| 0.75 | 0.44% (거의 안 들림) | **진다** |
| 1.50 | 17.78% | 판정 불가 |
| 3.00 (현재) | 23.45% | 이긴다 |

**"안 들린다"와 "잡음보다 낫다"를 동시에 만족하는 지점이 없다.**
촬영 전에 팀이 직접 들어보고 어느 쪽을 포기할지 정해야 한다.

```bash
python protect.py 팀원녹음.wav -o out/listen_075 --ratio 0.75
python protect.py 팀원녹음.wav -o out/listen_300 --ratio 3.0
# out/listen_*/original.wav 와 protected.wav 를 번갈아 들어볼 것
```

영상에서 원본과 보호본을 나란히 들려줄 계획이라면 **차이가 들리는 순간 신뢰를 잃는다.**
들린다면 배율을 낮추고, 대신 "잡음보다 낫다"는 주장을 접어야 한다.

### 3. 산출물을 미리 만들어 둔다

**실시간 처리를 카메라 앞에서 돌리지 말 것.** CPU에서 15스텝 0.91초/청크는
1.0초 예산에 여유가 거의 없고, 배경 프로세스가 있으면 밀린다.

실시간 장면은 **짧게** 넣고(숫자가 움직이는 것만), 정밀한 수치는 미리 만든 산출물로 보여준다.

```bash
# 모델·뱅크 캐시 (인터넷 없는 현장 대비)
python build_tts_bank.py
python -c "from mirinae.mode1.stt import SpeechToText; SpeechToText().transcribe_text(__import__('numpy').zeros(16000,dtype='float32'))"
```

---

## 촬영 구성 — 4막

### 1막 · 문제 (20초) — "6초면 복제된다"

1. 팀원 목소리 원본 재생 (자막: 실제 녹음)
2. 그 목소리로 만든 XTTS 복제본 재생 — **팀원이 한 적 없는 문장**으로
3. 두 파형을 나란히 놓고 화자 유사도 **0.95** 표시

이 장면은 실제로 잘 된다. 강하게 가도 된다.

```bash
.venv-xtts\Scripts\python clone_xtts.py out/demo/original.wav out/demo/clone_orig.wav \
    --text "안녕하세요 어머니 저 지금 급한 일이 생겨서 연락드렸어요"
```

### 2막 · 방어 (40초) — 숫자가 떨어진다

1. 앱 모드 2 화면 — 마이크 입력이 들어가고 **SRS 값이 실시간으로 내려가는** 장면
   (청크마다 갱신된다. 이게 유일한 "움직이는 그림"이다)
2. 원본 ↔ 보호본 청취 비교 — 2절에서 정한 배율로
3. 화자 검증기 판정 카드

```
                  SRS      판정
   원본           1.000    같은 화자
   보호본         0.634    다른 화자     ← 임계값 0.796
```

**임계값 0.796의 출처를 반드시 자막으로 넣을 것.** 근거 없는 숫자로 보이면 안 된다.
"서로 다른 화자 6명 · 조각쌍 664개에서 측정한 동일오류율 지점(4.3%)".

### 3막 · 대조군 (40초) — "그냥 잡음 아니냐"에 답한다

**이 막이 이 프로젝트에서 가장 강한 카드다.** 여기를 제일 공들여 찍는다.

같은 SNR 20 dB로 맞춘 네 가지를 나란히 놓는다.

| 조건 | SRS | 무슨 뜻인가 |
|---|---|---|
| C-C 무섭동 | 1.000 | 검증기가 정상 작동한다 |
| C-A 백색잡음 | 0.805 | 잡음만으로는 안 된다 |
| C-B 대역제한잡음 | 0.769 | 대역 제한이 전부가 아니다 |
| **C-E 셔플 섭동** | **0.890** | **크기는 같고 구조만 파괴 → 거의 무효** |
| **미리내** | **0.634** | |

C-E가 결정적이다. **최적화된 섭동을 시간축으로 블록 셔플만 해도 효과가 사라진다.**
크기가 아니라 **구조**가 원인이라는 뜻이고, "적대적 최적화를 실제로 했다"의 증거다.

여기에 통화 채널 통과 결과를 붙이면 더 강해진다.

| | 파일 기준 | **통화 채널 통과 후** |
|---|---|---|
| 미리내 | 0.634 | **0.592** |
| C-B 잡음 | 0.769 | 0.662 |

전화 통화(300~3400 Hz · 8 kHz · G.711)를 지나도 유지된다.

### 4막 · 한계 (30초) — 여기서 정직해야 한다

**복제 실패를 못 보여준다는 사실을 영상 안에서 직접 말한다.**

| 조건 | n | 복제음 SRS | DSR |
|---|---|---|---|
| 원본 | 5 | 0.9524 | 0% |
| 보호본 | 5 | 0.9122 | **0%** |
| C-B 잡음 | 5 | 0.8566 | **0%** |

그리고 **왜 그런지**를 말한다. 이게 기여다.

> 화자 임베딩을 남남보다 멀리 밀어냈는데도 복제음은 원본과 0.91이었다.
> **제로샷 복제 모델은 화자 임베딩 하나로 음색을 결정하지 않는다.**
> 임베딩을 표적으로 삼는 방어는 이 세대 모델에 구조적 한계가 있다.

숨기고 성공한 것처럼 편집하면 프로젝트의 존재 이유가 무너진다(D07 §07).
그리고 심사에서 한 번만 찔리면 나머지 수치의 신뢰도까지 같이 간다.

---

## 자막에 반드시 넣을 것

수치를 화면에 띄울 때는 **항상 세 가지를 같이** 낸다 — D09 §06이 못박은 형식이다.

```
n · 신뢰구간 · 대조군 대비
```

예: `보호본 SRS 0.634 (n=5, ±0.015) · C-B 대조군 0.769 대비 −0.135`

그리고 대상 음성이 무엇인지 자막으로 밝힌다.
팀원 녹음이면 "팀원 실제 녹음", 아직 합성음이면 **"XTTS 합성 화자 (사람 목소리 아님)"**.
이걸 안 밝히면 나중에 질문받았을 때 답이 궁색해진다.

---

## 촬영 당일 체크리스트

- [ ] 모델 캐시 확인 — 인터넷 없이 `protect.py`가 도는가
- [ ] TTS 뱅크 생성 — `python build_tts_bank.py`
- [ ] 배경 프로세스 정리 (실시간 장면이 밀린다)
- [ ] 산출물 미리 생성 — `out/demo/` 전체
- [ ] 원본·보호본 청취 확인 (당일 장비 스피커로)
- [ ] 임계값 0.796의 출처 자막 준비
- [ ] 4막 대본 확정 — 한계를 말하는 문장을 미리 써 둘 것

---

## 자주 나올 질문과 답

**"그냥 잡음 섞은 거 아닌가요?"**
→ 3막. C-A·C-B 대조군을 같은 SNR로 맞춰 놨고, 미리내가 더 낮다.
그리고 C-E 셔플이 거의 무효라는 것이 크기가 아니라 구조가 원인이라는 증거다.

**"안 들리나요?"**
→ 배율에 달렸고, 현재 설정(3.0)에서는 **bin의 20.3%가 마스킹 임계값을 넘는다.**
"안 들린다"고 단언하지 말 것. 청취 평가로 정한 배율과 그때의 위반 비율을 같이 말한다.

**"실제로 복제를 막나요?"**
→ **막지 못한다.** 4막에서 이미 말했어야 한다. 여기서 처음 나오면 안 된다.

**"임계값 0.796은 어디서 나왔나요?"**
→ 서로 다른 화자 6명의 3초 조각으로 같은 화자 쌍 116개, 타 화자 쌍 664개를 만들어
동일오류율 지점을 구했다(4.3%). 다만 XTTS 합성 화자 기반이라 잠정값이다.

**"CPU로 되나요?"**
→ 오프라인 보호는 된다. 실시간은 15스텝에서 0.91초/청크로 **경계선**이다.
GPU가 있으면 200스텝을 그대로 쓴다.
