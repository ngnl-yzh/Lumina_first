# 모드 2 시연 영상 — 제작 문서

목표 길이 **2분 30초**. 나레이션 대본, 화면 구성, 자막, 준비 명령을 씬 단위로 적었다.
그대로 읽고 그대로 찍으면 된다.

> **먼저 알아야 할 것 — 복제 실패 장면은 찍을 수 없다.**
> 보호본을 XTTS-v2에 넣어도 복제는 성공한다. DSR 0% (n=5, 원본·보호본·잡음 대조군 전부).
> 계획서(D07 §04)가 클라이맥스로 상정한 장면이 **현재 존재하지 않는다.**
> 이 대본은 그 사실 위에서 짜여 있다. 씬 7이 그 자리를 대신한다.

---

# 1부 · 사전 준비

촬영 **전날까지** 끝낸다. 당일에 만들면 반드시 뭔가 어긋난다.

## 준비 1 — 팀원 목소리 녹음 (가장 중요)

지금까지의 모든 수치는 XTTS 내장 화자 기반이다. 그건 "XTTS가 만든 것을 XTTS가 복제한다"라
복제가 유난히 쉬운 조건이고, 심사에서 "대상 음성이 뭐냐"고 물으면 답이 궁색해진다.

- 조용한 방, **30초 이상**, 16 kHz 이상 모노
- 파일명 `demo_voice.wav`
- **읽을 내용** — 시연에서 복제할 문장과 겹치지 않게 한다

> "안녕하세요. 오늘은 날씨가 참 좋네요. 점심에는 근처 식당에서 밥을 먹고,
> 오후에는 도서관에 들러 책을 몇 권 빌릴 생각입니다.
> 저녁에는 가족들과 함께 산책을 하려고 합니다."

## 준비 2 — 마스킹 배율을 귀로 정한다

**이 결정을 안 하고 촬영에 들어가면 안 된다.**
씬 3에서 원본과 보호본을 나란히 들려주는데, 차이가 들리는 순간 신뢰를 잃는다.

```bash
cd server
python protect.py demo_voice.wav -o out/listen_075 --ratio 0.75 --steps 200
python protect.py demo_voice.wav -o out/listen_300 --ratio 3.0  --steps 200
```

`original.wav`와 `protected.wav`를 **촬영 당일 쓸 스피커로** 번갈아 들어본다.

| 배율 | 마스킹 임계값 위반 bin | 잡음 대비 |
|---|---|---|
| 0.75 | 0.44% — 거의 안 들린다 | **진다** |
| 3.00 | 23.45% — 들릴 수 있다 | 이긴다 |

**둘 다 만족하는 값은 없다.** 팀이 정한다.

- **0.75를 고르면** → 씬 5(대조군)의 우위 주장을 **접어야 한다.** 대본에서 씬 5를 빼고,
  씬 7에 "현재 설정은 잡음 대조군을 이기지 못한다"를 추가한다.
- **3.0을 고르면** → 씬 3의 청취 비교에서 차이가 들릴 수 있다.
  들린다면 청취 비교를 빼고 숫자만 보여준다.

아래 대본은 **배율 3.0 기준**으로 썼다.

## 준비 2-B — 복제 서비스로 실제 검증 (권장)

**복제 모델을 이 프로그램에 넣지 않는다.** 사용자가 직접 외부 서비스에 올린다.
시연에서 "우리 도구로 우리가 검증했다"가 아니라
**"제3자 서비스에 실제로 넣어봤다"** 가 되어 반박하기 훨씬 어렵다.

### 어떤 모델을 쓰나 — GPT-SoVITS

한국어 복제는 **GPT-SoVITS**가 사실상 표준이다. 무료·오픈소스이고,
**WebUI가 있어서 파일을 올리고 버튼만 누르면 된다** — 코드가 필요 없다.
5초 샘플로 제로샷이 되고, 1분이면 미세조정까지 된다.

대안: Zonos, CosyVoice2. 한국어는 GPT-SoVITS 쪽 평이 낫다.
ElevenLabs 같은 상용은 마지막에 보조로 — 블랙박스라 진단이 안 되고,
반복 측정이 크레딧에 걸리며, 모델이 조용히 바뀌면 재현이 안 된다.

### 순서

1. 앱 **딥보이스 방지** 탭에서 녹음 → **원본 · 보호본 · 대조군** 세 파일 저장
2. GPT-SoVITS에 **세 파일을 각각 따로** 올려 목소리 학습
3. 셋 모두 **같은 문장**으로 음성 생성 — **조건당 3~5번 반복**
4. 생성된 파일 전부를 앱 **복제 검증** 탭에 넣고 실행

3번의 반복이 중요하다. 생성이 확률적이라 1회로는 착시가 난다 —
실측에서 완전히 같은 파일을 두 번 복제했는데 유사도가 0.019 차이 났고,
방어 효과는 0.027~0.047 수준이었다. **같은 자릿수다.**

### 화면이 알아서 판정한다

- 원본 복제 유사도가 낮으면 **"이 모델은 이 목소리를 애초에 복제하지 못했다"** 고 먼저 알린다.
  기준선이 없으면 방어 효과를 논할 수 없다.
- 보호본이 임계값 위면 "복제를 막지 못했다"
- 보호본이 잡음 대조군보다 높으면 **"그냥 잡음 아니냐에 답할 수 없다"**
- 조건당 표본이 3개 미만이면 경고

이 결과를 그대로 화면 녹화하면 씬 7의 근거가 된다.

## 준비 3 — 촬영용 산출물 일괄 생성

```bash
cd server

# ① 보호본 + 대조군 (씬 3~5)
python protect.py demo_voice.wav -o out/demo --ratio 3.0 --steps 200

# ② 원본 복제 — 씬 1의 "가짜 목소리"
.venv-xtts\Scripts\python clone_xtts.py out/demo/original.wav out/demo/clone_orig.wav \
    --text "어머니 저예요. 지금 급한 일이 생겨서 연락드렸어요. 잠시만 통화 괜찮으세요?"

# ③ 보호본 복제 — 씬 7의 "그래도 복제된다"
.venv-xtts\Scripts\python clone_xtts.py out/demo/protected.wav out/demo/clone_prot.wav \
    --text "어머니 저예요. 지금 급한 일이 생겨서 연락드렸어요. 잠시만 통화 괜찮으세요?"

# ④ 판정 기준선 (씬 4 자막 근거)
python calibrate_dsr.py out/speakers --reference out/demo/original.wav \
    --compare out/demo/protected.wav out/demo/control_C-A.wav \
              out/demo/control_C-B.wav out/demo/control_C-C.wav out/demo/control_C-E.wav

# ⑤ 통화 채널 통과 (씬 6)
python codec_test.py out/demo
```

**④와 ⑤의 터미널 출력을 그대로 화면 녹화한다.** 표를 다시 그리지 말 것 —
도구가 낸 원본 출력이 훨씬 신뢰감을 준다.

## 준비 4 — 앱 화면 녹화 (씬 3용)

```bash
# 터미널 1
python ws_server.py --port 8765
# 터미널 2
cd app-desktop && npm run dev
```

앱에서 **딥보이스 방지** 탭 → 녹음 시작 → 준비 1의 문장을 읽고 → 녹음 중지.
정지하면 **보호 진행** 막대가 차오르고, 끝나면 SRS와 판정이 뜬다.
녹화는 그 구간만 하면 된다.

> **실시간 주입이 아니다.** 녹음을 마쳐야 최적화가 시작된다.
> 청크로 쪼개 실시간 처리하던 방식은 **방어가 성립하지 않아** 뺐다.
> 실측(14초 녹음 · 60스텝 · 통화채널 통과 후):
>
> | 방식 | SRS | 소요 |
> |---|---|---|
> | 청크 스트리밍 (옛 방식) | 0.8045 — 임계값 위. 실패 | 763초 |
> | 전체 발화 일괄 (현재) | **0.3709** — 성공 | 56초 |
>
> 나레이션에서 "통화하는 동안 실시간으로"라고 말하면 안 된다.
> 씬 3 대본이 그에 맞게 쓰여 있는지 확인할 것.

---

# 2부 · 촬영 대본

각 씬은 **화면 / 자막 / 나레이션** 세 줄로 되어 있다.
나레이션은 인용부호 안을 그대로 읽는다.

---

## 씬 1 · 후크 (0:00 – 0:22)

**화면**
검은 배경에 파형 두 개. 왼쪽 파형이 재생되며 빛나고, 이어서 오른쪽 파형이 재생된다.
둘 다 라벨 없음.

**오디오**
`out/demo/original.wav` (5초) → 0.5초 공백 → `out/demo/clone_orig.wav` (5초)

**나레이션**
> "지금 들으신 두 목소리 중 하나는 사람이 아닙니다.
> 6초 분량의 녹음만 있으면, 하지 않은 말을 시킬 수 있습니다."

**자막**
```
왼쪽 — 실제 녹음
오른쪽 — AI 합성 (본인이 한 적 없는 문장)
```

---

## 씬 2 · 문제 (0:22 – 0:45)

**화면**
공격 사슬 4단계 다이어그램이 왼쪽부터 차례로 나타난다.

```
목소리 수집  →  음성 복제  →  가족 사칭 통화  →  송금
```

`가족 사칭 통화` 아래에 "기존 대응은 여기서부터"가 붙고 그 구간만 밝아진다.
그 다음 `목소리 수집` 아래에 "미리내 모드 2"가 붙으며 그쪽이 밝아진다.

**나레이션**
> "지금까지의 대응은 대부분 통화가 시작된 뒤에 작동합니다.
> 그런데 목소리 복제는 이미 그 전에 끝나 있습니다.
> 미리내 모드 2는 사슬의 맨 앞, 목소리가 수집되는 순간에 개입합니다."

**자막**
```
방어 지점을 앞으로 옮긴다
```

---

## 씬 3 · 원리와 보호 처리 (0:45 – 1:20)

**화면 (전반 15초)**
스펙트로그램 위에 마스킹 임계값 곡선이 그려지고, 그 **아래**에 섭동이 채워지는 애니메이션.

**나레이션**
> "통화하는 동안, 목소리에 아주 작은 신호를 겹칩니다.
> 큰 소리 옆의 작은 소리는 들리지 않는다는 성질을 이용해,
> 들리지 않는 한계선 아래로 숨깁니다.
> 사람에게는 아무 의미 없는 신호지만, 목소리의 주인을 식별하는 AI에게는
> 지문을 흐트러뜨리는 신호입니다."

**화면 (후반 20초)**
준비 4에서 녹화한 앱 화면. 녹음을 마치면 **보호 진행 막대가 차오르고
SRS와 판정이 뜨는** 구간을 그대로 보여준다.

**나레이션**
> "녹음을 마치면 발화 전체를 한 번에 처리합니다.
> 화면의 숫자가 목소리의 유사도입니다. 낮을수록 다른 사람으로 들린다는 뜻입니다."

> **연출 메모** — "통화하는 동안 실시간으로"라고 말하지 말 것.
> 청크로 쪼개 실시간 처리하는 방식은 방어가 성립하지 않아 뺐다(준비 4 참조).
> 실시간을 주장하면 씬 7의 정직함과 정면으로 충돌한다.

**자막**
```
심리음향 마스킹 — 들리지 않는 한계선 아래로
```

**청취 비교** (준비 2에서 "안 들린다"고 판단했을 때만)
`out/demo/original.wav` 3초 → `out/demo/protected.wav` 3초, 자막 `원본 / 보호본`

> **들린다고 판단했으면 이 부분을 통째로 뺀다.** 억지로 넣으면 그 자리에서 신뢰를 잃는다.

---

## 씬 4 · 판정 (1:20 – 1:40)

**화면**
준비 3-④의 `calibrate_dsr.py` 터미널 출력을 녹화한 것. 아래 두 줄을 강조 표시한다.

```
protected.wav       SRS 0.6342  → 타 화자 판정
DSR 판정 임계값 (EER 지점): 0.7962   동일오류율 4.3%
```

**나레이션**
> "화자 검증기에 넣어 보겠습니다.
> 보호본은 0.634. 서로 다른 사람이라고 판정하는 기준선 0.796보다 낮습니다.
> 이 기준선은 저희가 정한 숫자가 아니라, 서로 다른 화자 여섯 명의
> 음성 조각 664쌍에서 측정한 값입니다."

**자막**
```
판정 기준선 0.796
서로 다른 화자 6명 · 조각쌍 664개 · 동일오류율 4.3%
```

---

## 씬 5 · 대조군 — 이 영상에서 가장 중요한 장면 (1:40 – 2:10)

**화면**
막대 그래프. 같은 SNR 20 dB로 맞춘 다섯 조건이 왼쪽부터 하나씩 나타난다.
`0.796` 위치에 점선(판정 기준선). 기준선 아래로 내려간 막대만 색이 바뀐다.

| 조건 | SRS |
|---|---|
| C-C 무섭동 | 1.000 |
| C-A 백색잡음 | 0.805 |
| C-B 통화대역 잡음 | 0.769 |
| **C-E 셔플 섭동** | **0.890** |
| **미리내** | **0.634** |

**나레이션**
> "그냥 잡음을 섞은 것과 뭐가 다르냐는 질문이 남습니다.
> 같은 세기로 맞춘 백색잡음은 0.805, 통화 대역 잡음은 0.769.
> 둘 다 미리내보다 높습니다.
>
> 결정적인 것은 이겁니다.
> 미리내가 만든 바로 그 신호를, 세기는 그대로 두고 **시간 순서만 뒤섞어** 봤습니다.
> 0.890. 효과가 거의 사라집니다.
>
> 세기가 아니라 **구조**가 원인이라는 뜻입니다."

**자막**
```
C-E — 같은 신호, 시간 순서만 뒤섞음
세기가 아니라 구조가 효과를 만든다
```

> **연출 메모** — C-E 막대가 나타날 때 한 박자 쉰다. 이 장면이 이 프로젝트의 핵심 논거다.

---

## 씬 6 · 통화 채널 (2:10 – 2:22)

**화면**
준비 3-⑤의 `codec_test.py` 출력. 아래 두 줄 강조.

```
보호본        0.6342 → 0.5915
C-B 잡음      0.7685 → 0.6620
```

옆에 채널 구성을 작게 표시: `300~3400 Hz · 8 kHz · G.711`

**나레이션**
> "실제 통화는 좁은 대역과 코덱을 지납니다.
> 그 경로를 그대로 통과시켜도 0.592로 유지됩니다. 잡음 대조군보다 낮습니다."

**자막**
```
전화망을 통과해도 유지된다
```

---

## 씬 7 · 한계 — 여기서 정직해야 한다 (2:22 – 2:50)

**화면**
표 하나. 배경색을 앞 씬들과 다르게 해서 전환을 분명히 한다.

| 조건 | n | 복제음 유사도 | 방어 성공률 |
|---|---|---|---|
| 원본 | 5 | 0.9524 | 0% |
| **보호본** | 5 | **0.9122** | **0%** |
| C-B 잡음 | 5 | 0.8566 | 0% |

**오디오**
`out/demo/clone_prot.wav` 4초 재생. 자막 `보호본으로 만든 복제음`

**나레이션**
> "여기서 정직하게 말씀드릴 것이 있습니다.
>
> 보호한 음성을 실제 복제 모델에 넣고 다섯 번 반복 측정했습니다.
> **복제는 성공했습니다.**
> 화자 검증기 기준으로는 남남보다 멀리 밀어냈는데도 그렇습니다.
>
> 이것은 제로샷 복제 모델이 화자 임베딩 하나만으로 음색을 결정하지 않는다는 뜻입니다.
> 임베딩을 표적으로 삼는 방어에는 구조적 한계가 있습니다.
>
> 저희는 그 한계를 숨기지 않고 숫자로 확인했습니다."

**자막**
```
현재 한계 — 복제 자체는 막지 못한다
```

---

## 씬 8 · 마무리 (2:50 – 3:00)

**화면**
검은 배경에 텍스트 세 줄이 차례로.

```
화자 식별은 무너뜨렸다
복제는 아직 막지 못한다
왜 막지 못하는지를 남긴다
```

**나레이션**
> "막지 못했다는 사실과, 왜 막지 못하는지를 함께 남깁니다.
> 그것이 다음 사람이 이어받을 수 있는 결과라고 생각합니다."

**자막**
```
미리내 · Team Lumina · 전남대학교 인공지능학부
```

---

# 3부 · 촬영 순서

산출물이 준비되어 있다는 전제로, 이 순서가 가장 빠르다.

1. **터미널 화면 녹화** (씬 4·6) — 준비 3의 ④⑤를 다시 실행하며 녹화.
   폰트를 키우고(14pt 이상) 창을 좁혀 글자가 화면을 채우게 한다.
2. **앱 화면 녹화** (씬 3 후반) — 녹음 → 정지 → 보호 진행 → 판정까지 한 번에 찍는다.
3. **오디오 클립 정리** (씬 1·3·7) — 재생 구간을 미리 잘라 둔다.
4. **나레이션 녹음** — 화면 없이 전 씬을 한 번에. 씬 5는 두세 번 찍어 가장 또렷한 것을 쓴다.
5. **그래픽 제작** (씬 2·5·7·8) — 표와 다이어그램.
6. **편집·자막**

---

# 4부 · 편집 노트

**수치를 띄울 때는 항상 셋을 같이 낸다** — D09 §06이 못박은 형식이다.

```
n · 신뢰구간 · 대조군 대비
```

예: `보호본 0.634 (n=5, ±0.015) · 통화대역 잡음 0.769 대비 −0.135`

**대상 음성이 무엇인지 반드시 밝힌다.**
팀원 녹음이면 `실제 녹음`, 합성음이면 `XTTS 합성 (사람 목소리 아님)`.
이걸 빼면 나중에 질문받았을 때 답이 궁색해진다.

**하지 말 것**
- "완벽하게 차단" 같은 표현 — 씬 7과 정면으로 충돌한다
- 대조군 없이 SRS 숫자만 띄우기 — "그냥 잡음 아니냐"에 답할 수 없다
- 씬 7을 빼거나 뒤로 미루기 — 심사에서 한 번 찔리면 나머지 수치 신뢰도까지 같이 간다

---

# 5부 · 촬영 당일 체크리스트

- [ ] 인터넷 없이 `protect.py`가 도는가 (모델 캐시)
- [ ] `python build_tts_bank.py` 완료
- [ ] `out/demo/` 산출물 전체 생성 확인
- [ ] 당일 스피커로 원본·보호본 청취 확인 → 씬 3 청취 비교 넣을지 결정
- [ ] 배경 프로세스 종료 (보호 처리가 빨라진다)
- [ ] 터미널 폰트 크기 확인
- [ ] 씬 7 나레이션을 소리내어 한 번 읽어보기 — 가장 어려운 대사다

---

# 부록 · 예상 질문과 답

**"그냥 잡음 섞은 거 아닌가요?"**
→ 씬 5. 같은 SNR로 맞춘 백색잡음·통화대역잡음보다 낮고,
같은 신호를 시간 순서만 뒤섞으면 효과가 사라진다(0.890). 구조가 원인이다.

**"안 들리나요?"**
→ 배율에 달렸다. 현재 설정(3.0)에서는 **STFT bin의 20.3%가 마스킹 임계값을 넘는다.**
"안 들린다"고 단언하지 말고, 청취 평가로 정한 배율과 그때의 위반 비율을 같이 말한다.

**"실제로 복제를 막나요?"**
→ **막지 못한다.** 씬 7에서 이미 말했다. 여기서 처음 나오면 안 된다.

**"기준선 0.796은 어디서 나왔나요?"**
→ 서로 다른 화자 6명의 3초 조각으로 같은 화자 쌍 116개, 타 화자 쌍 664개를 만들어
동일오류율 지점을 구했다(4.3%). 다만 XTTS 합성 화자 기반이라 잠정값이다.

**"CPU로 되나요?"**
→ 된다. 다만 **실시간 주입은 하지 않는다** — 녹음을 마친 뒤 전체를 한 번에 처리한다.
청크로 쪼개 실시간 처리하는 방식은 방어가 성립하지 않아 뺐다.
14초 녹음에 CPU 약 1분, GPU면 수 초다.

**"왜 복제를 못 막는데 발표하나요?"**
→ 공격 사슬의 비어 있는 앞단에 방어를 배치하고, 그것이 어디까지 작동하는지
정직하게 측정하는 것이 이 프로젝트의 목적이다(D07 §07).
"임베딩 표적 방어는 이 세대 모델에 구조적 한계가 있다"는 정량적 발견은
다음 시도가 어디로 가야 하는지를 알려준다.
