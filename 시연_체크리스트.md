# 시연 당일 체크리스트

한 장으로 끝낸다. 위에서부터 순서대로.

---

## 0 · 가장 쉬운 길 — EXE

EXE가 **두 가지**다. 쓰임이 다르다.

| | 파일 | 크기 | 기동 | 쓸 곳 |
|---|---|---:|---:|---|
| **폴더 배포판** | `server\dist\미리내\미리내.exe` | 953 MB (폴더 전체) | **40초** | **시연 당일** |
| **단일 파일** | `server\dist\미리내_단일파일.exe` | **350 MB** | 2~4분 | **남에게 건넬 때** |

둘 다 더블클릭하면 서버(8765)와 화면(8080)이 함께 뜬다.
브라우저에서 **http://localhost:8080** 을 연다. 파이썬·npm 설치가 필요 없다.

> **시연에는 폴더 배포판을 쓸 것.** 단일 파일은 실행할 때마다 내용물을
> 임시 폴더에 푸는 구조라 기동이 훨씬 느리다. 대신 **파일 하나만 건네면 되므로**
> 공유·제출용으로는 그쪽이 맞다.

> **EXE의 모드 2도 이제 진짜 방어 경로로 돈다.** 예전에는 검증기 경로로
> 돌았고 그건 실제 복제를 막지 못했다. 지금은 GPT-SoVITS를 직접 표적으로
> 삼는다 — 가중치 `s2G488k.pth`(약 100 MB)를 **첫 실행에서 내려받으므로
> 처음 한 번은 인터넷이 필요하다.** 내려받은 뒤에는 오프라인으로 돈다.
> 망이 없으면 옛 검증기 경로로 물러선다(그 경우 실제 저지는 못 한다).

**모드 2까지 보여줄 거면** 최상위의 배치 파일을 더블클릭한다.

```
미리내_실행.bat
```

번호를 물어본다 — `1`은 모드 1만(빠름), `2`는 모드 2까지(2~3분).
서버 창과 앱 창이 각각 뜨고, 앱은 **http://localhost:5173** 이다.

---

## 1 · 손으로 띄우기 (시연 30분 전)

**모드 1과 모드 2는 서버가 다르다.** 둘 다 필요하면 포트를 나눠 띄운다.

```bash
cd server
```

**모드 1만 보여줄 때** — 가볍고 빠르다.

```bash
.venv/Scripts/python ws_server.py --port 8765
```

**모드 2도 보여줄 때** — 기동 1~2분, 보호 자체는 **15초**다.

```bash
.venv-xtts/Scripts/python ws_server.py --port 8765 --cloner gsv --steps 400
```

**실측 (8초 녹음 · CPU · 400스텝) — 15초.** 예전 80스텝 2~3분에서
25배 빨라졌다. XTTS를 함께 올리고 운율 경로까지 돌던 것을 걷어냈기
때문이다. GPT-SoVITS의 `ref_enc`는 음색만 보므로 운율 가중치가 0이면 된다.

**표적을 `xtts,gsv`로 되돌리지 말 것.** 느려질 뿐 아니라 예산이 나뉘어
저지율이 떨어진다 — 표적 3개 150스텝은 저지 0%, GPT-SoVITS 집중은 100%였다.

> 화면에 `서버 시작: ws://0.0.0.0:8765`가 뜰 때까지 기다린다.
> 그 전에 앱에서 누르면 연결 실패로 보인다.

**앱**

```bash
cd app-desktop && npm run dev
```

---

## 2 · 모드 1 시연 — 보이스피싱 탐지

**딥보이스 방지 탭이 아니라 `보이스피싱 감지` 탭이다.** `모니터링 시작`을 누른다.

읽을 대본 (한 문장씩 또박또박, 사이에 1초 쉼):

> "서울중앙지검 첨단범죄수사부입니다."
> "선생님 명의 통장이 자금세탁에 이용된 정황이 확인됐습니다."
> "수사 기밀이니 가족에게도 말씀하지 마시고 조용한 곳으로 이동해 주세요."
> "안전계좌로 오늘 안에 이체하셔야 합니다."

세 번째 마디쯤에서 위험도가 올라가고 경고가 뜬다.

### 여기서 조심할 것

- **STT가 깨지면 점수가 안 오를 수 있다.** 실제로 `금융감독원`이 "금융갈리원"으로,
  `안전계좌`가 "안 심계자"로 전사된 사례가 있었다. 그 오류형은 지금 DB에 넣어 뒀지만
  **다른 방식으로 깨지면 못 잡는다.**
- 그래서 **또박또박, 조용한 곳에서** 읽는다. 마이크를 입 가까이.
- 안 잡히면 당황하지 말고 다음 문장을 이어 읽는다. 증거가 누적되는 구조다.

### 질문이 오면

> **"정확도가 얼마인가요?"**
> 조건을 먼저 말씀드려야 합니다. 저희가 **적대적으로 설계한** 검증셋에서
> 45.5%였고, 그걸 보고 고쳐서 지금은 더 높습니다. 다만 고친 뒤 숫자는
> 훈련 성적이라 **45.5%를 기준**으로 봐 주십시오.

> **"오탐은요?"**
> 정상 통화 172건에서 0.58%입니다. 다만 그 숫자보다 중요한 게 있습니다 —

---

## 3 · 가장 강한 카드 — "언제 켜는가"

**이 표 하나가 이 프로젝트에서 가장 강합니다.**

| 언제 켜는가 | 통화 1000건 중 사기 | 경고가 진짜일 확률 |
|---|---:|---:|
| 모든 통화 | ~1건 | **14.6%** |
| **모르는 번호만** | ~20건 | **77.7%** |
| 사용자가 이상하다 느껴 켤 때 | ~100건 | **95.0%** |

말할 내용:

> "같은 프로그램인데 **언제 켜느냐로 14%와 95% 사이**를 오갑니다.
> 모든 통화에 켜면 경고 일곱 번 중 한 번만 진짜입니다. 성능 문제가 아니라 산수입니다.
>
> 그래서 저희는 **모르는 번호에만 켜는 설계**를 택했습니다.
> 그런데 발신번호는 **통신사만 가진 정보**입니다.
> 이건 통신사가 아니면 못 하는 제안입니다."

재현이 필요하면 그 자리에서: `python eval_realuse.py`

---

## 4 · 모드 2 시연 — 딥보이스 방지

`딥보이스 방지` 탭 → **녹음 시작** → 6초쯤 말하고 → **녹음 중지**.

진행 막대가 차오르고 끝나면 **복제기 조건** 값과 **섭동** 판정이 뜬다.
CPU에서 **약 60초**. 기다리는 동안 원리를 설명한다.

> **화면 문구를 정확히 읽을 것.** "복제기 조건"은 화자 검증기 점수가 아니라
> **복제 모델 내부의 화자 조건** 유사도다. 판정은 "다른 화자"가 아니라
> **"섭동이 충분히 들어갔나"** 다 — 그 값이 복제 저지를 보장하지는 않는다.

> "복제 모델이 목소리를 읽는 통로를 직접 겨냥합니다.
> 화자 검증기를 속이는 방식으로는 **네 번 시도해 네 번 다 실패했습니다.**
> 표적을 옮기고 나서야 뚫렸습니다."

### 이제 말할 수 있는 것 — 모드 3

> "**GPT-SoVITS에 원본을 주면 그 사람 목소리가 나오고,
> 보호본을 주면 다른 사람 목소리가 나옵니다.**
>
> 조건 45개에서 되풀이했습니다. 검증기 셋이 **전부** 임계값 아래로
> 내려간 경우만 저지로 셉니다. 최종 설정에서 **한 겹 열화까지 13개 중 12개**를
> 막았습니다 — mp3, 전화대역, 잡음, 3초 절단을 각각 거친 뒤에도요."

**핵심은 EOT입니다.** 이 이야기가 가장 강합니다.

> "처음엔 손잡이를 돌렸습니다. 스텝을 늘리고 섭동을 키웠죠.
> **더 나빠졌습니다.** mp3 조건이 0.84에서 0.90으로 갔습니다.
>
> 그래서 진단을 바꿨습니다. 세기의 문제가 아니라 **강건성**의 문제였습니다.
> 깨끗한 파일에서만 최적화한 섭동은 카카오톡을 한 번 거치면 씻겨 나갑니다.
>
> 그래서 **열화를 최적화 안으로** 넣었습니다. 매 스텝 전화대역을 씌우거나
> 잡음을 얹거나 잘라 낸 상태에서 손실을 잽니다. 그러자 전부 막혔습니다."

### 반드시 지킬 선

**"어떤 딥보이스도 막는다"고 말하지 않는다.** 확인한 범위는 이렇다.

- 복제 모델은 **GPT-SoVITS** 하나다. XTTS는 이 설정으로 재보지 않았다.
- **4단 겹침(mp3+전화+잡음+2초 절단)은 못 막는다.** 다만 그 조건에서는
  원본조차 복제되지 않아 측정이 성립하지 않는다.
- 화자 6명 중 **1명이 남았다** (Resemblyzer 0.8384 대 임계값 0.8090).

> "저희가 확인하지 못한 것도 말씀드리겠습니다 — 겹겹이 망가진 파일에서는
> 섭동이 사라집니다. 다만 그때는 원본을 줘도 복제가 안 됩니다."

---

## 5 · 실시간 질문이 오면

> **"실시간으로 되나요?"**
>
> 두 가지 방식으로 시도해 **둘 다 측정으로 부정했습니다.**
> 복제 모델이 화자 특징을 뽑는 창이 수 초 단위여서,
> 그보다 짧게 잘라 처리하면 방어가 성립하지 않습니다.
>
> 대신 **목소리가 저장되는 지점**에 붙이는 형태를 택했습니다 —
> 통화 녹음 서비스, 음성사서함, SNS 업로드 전.
> 셋 다 실시간이 필요 없습니다.

재현: `python eval_universal.py` · `python eval_streaming.py`

---

## 6 · 미리 준비해 둘 파일

시연 중 생성이 실패할 때를 대비해 **미리 만들어 둔 것**을 재생한다.

| 파일 | 용도 |
|---|---|
| `server/out/team_30/original.wav` | 원본 |
| `server/out/team_30/protected.wav` | 보호본 (배율 3.0) |
| `server/out/sweep/E-EOT/clone_from_original_0.wav` | **원본**을 GPT-SoVITS로 복제 — 본인 목소리가 난다 |
| `server/out/sweep/E-EOT/clone_from_protected_0.wav` | **보호본**을 복제 — 다른 사람이 난다 |
| `server/out/sweep/E-EOT-phone/clone_from_protected_0.wav` | 전화대역을 거친 뒤에도 막힌다 |
| `server/out/sweep/E-EOT-mp3/clone_from_protected_0.wav` | mp3를 거친 뒤에도 막힌다 |

**이 네 개를 나란히 들려주는 게 가장 강하다.** 숫자보다 귀가 먼저 안다.
재현: `.venv/Scripts/python score_sweep.py`

---

## 7 · 사고 대응

| 증상 | 대처 |
|---|---|
| 앱이 "서버 대기"에서 안 바뀜 | 서버 로그에 `서버 시작`이 떴는지 확인. 모드 2는 기동 2~3분 |
| 모드 1이 0%에서 안 오름 | 당황 말고 다음 문장 이어 읽기. 증거는 누적된다 |
| 모드 2 보호가 너무 느림 | `--steps 150`으로 낮춘다. EOT 150스텝도 저지가 확인됐다 |
| EXE 모드 2가 검증기 경로로 물러섬 | 인터넷이 없어 `s2G488k.pth`를 못 받은 것이다. 한 번은 망이 필요하다 |
| 판정이 "부족"으로 뜸 | 정상이다. 스텝이 모자란 것이지 고장이 아니다. 80스텝이면 "충분"이 나온다 |
| 복제 결과를 못 만듦 | 6번의 준비 파일로 대체 |

---

## 8 · 한 문장으로 정리

> **모드 1** — 통신사가 발신번호로 걸러 주면 경고 넷 중 셋이 진짜입니다.
> **모드 2·3** — 목소리가 저장되는 곳에서 미리 막습니다.
> GPT-SoVITS로 실제 복제해 봤고, 한 겹 열화까지 13개 중 12개를 막았습니다.
> 열화를 최적화 안에 넣은 것(EOT)이 갈랐습니다.
>
> 저희가 확인한 것과 확인하지 못한 것을 구분해서 말씀드렸습니다.
