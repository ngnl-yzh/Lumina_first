[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning

# 통합 테스트는 PGD를 실제로 돌리므로 느리다.
# 빠른 확인만 하려면: pytest -m "not slow"
markers =
    slow: 실제 모델을 돌리는 통합 테스트
